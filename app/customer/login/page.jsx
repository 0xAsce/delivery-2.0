"use client";
import { useState } from "react";
import Link from "next/link";

export default function CustomerLogin() {
  const [phone, setPhone] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(e) { e.preventDefault(); setError(""); setBusy(true); try { const r = await fetch("/api/customer/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ phone, password }) }); const d = await r.json(); if (!r.ok) { setError(d.error || "Login failed."); return; } window.location.href = "/customer/profile"; } catch { setError("Unable to connect to the server."); } finally { setBusy(false); } }
  return <main className="min-h-screen bg-[#f7f8f4] p-4 flex items-center justify-center"><form onSubmit={submit} className="w-full max-w-md bg-white rounded-3xl p-6 shadow-sm border border-gray-200 space-y-4"><div><h1 className="text-2xl font-black">Customer login</h1><p className="text-gray-500 mt-1">Sign in with your phone number and password.</p></div>{error && <div className="rounded-xl bg-red-50 text-red-700 p-3">{error}</div>}<input required value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="Phone number" className="w-full rounded-xl border p-3" inputMode="tel" /><input required type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Password" className="w-full rounded-xl border p-3" /><div className="text-right"><Link className="text-sm underline" href="/customer/reset-password">Forgot password?</Link></div><button disabled={busy} className="w-full rounded-xl bg-black text-white p-3 font-bold">{busy ? "Signing in…" : "Log in"}</button><p className="text-sm text-center">New customer? <Link className="underline font-bold" href="/customer/register">Create an account</Link></p></form></main>;
}